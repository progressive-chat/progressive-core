// progressive_combined_02.cpp — Progressive Native Combined Module 2
// Progressive Chat v0.3.5 — unified compilation unit (7 modules merged)
#include "progressive/json_parser.hpp"
#include <string>
#include <vector>
#include <sstream>
#include <algorithm>
#include <map>
#include <chrono>
#include <random>

using progressive::parseJsonStringValue;
using progressive::parseJsonBoolValue;
using progressive::parseJsonInt64Value;

std::string p08_Fn08A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p08_Fn08O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p08_Fn08O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p08_Fn08O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p09_Fn09O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p09_Fn09O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p09_Fn09O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p10_Fn10O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p10_Fn10O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p10_Fn10O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p11_Fn11O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p11_Fn11O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p11_Fn11O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p12_Fn12O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p12_Fn12O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p12_Fn12O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p13_Fn13O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p13_Fn13O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p13_Fn13O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p14_Fn14O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p14_Fn14O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p14_Fn14O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}
