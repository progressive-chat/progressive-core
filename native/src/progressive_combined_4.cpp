// progressive_combined_04.cpp — Progressive Native Combined Module 4
// Progressive Chat v0.3.5 — unified compilation unit (7 modules merged)
#include "progressive/json_parser.hpp"
#include <string>
#include <vector>
#include <sstream>
#include <algorithm>
#include <map>
#include <chrono>
#include <random>

using progressive::parseJsonStringValue;
using progressive::parseJsonBoolValue;
using progressive::parseJsonInt64Value;

std::string p22_Fn22A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p22_Fn22O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p22_Fn22O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p22_Fn22O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p23_Fn23O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p23_Fn23O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p23_Fn23O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p24_Fn24O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p24_Fn24O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p24_Fn24O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p25_Fn25O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p25_Fn25O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p25_Fn25O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p26_Fn26O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p26_Fn26O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p26_Fn26O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p27_Fn27O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p27_Fn27O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p27_Fn27O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28A(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28A"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28A" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28B(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28B"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28B" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28C(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28C"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28C" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28D(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28D"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28D" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28E(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28E"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28E" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28F(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28F"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28F" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28G(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28G"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28G" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28H(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28H"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28H" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28I(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28I"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28I" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28J(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28J"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28J" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28K(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28K"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28K" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28L(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28L"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28L" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28M(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28M"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28M" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28N(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28N"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28N" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}

std::string p28_Fn28O(const std::string& json) {
    if (json.empty()) return R"({"ok":false,"fn":"p28_Fn28O"})";
    std::string id = parseJsonStringValue(json, "id");
    std::string type = parseJsonStringValue(json, "type");
    std::string val = parseJsonStringValue(json, "value");
    int64_t ts = parseJsonInt64Value(json, "ts");
    int64_t n = parseJsonInt64Value(json, "n");
    bool flag = parseJsonBoolValue(json, "flag");
    std::ostringstream o;
    o << R"({"fn":")" << "p28_Fn28O" << R"(","ok":true)";
    if (!id.empty()) o << R"(,"id":")" << id << R"(")";
    if (!type.empty()) o << R"(,"type":")" << type << R"(")";
    if (!val.empty()) o << R"(,"value":")" << val << R"(")";
    if (ts > 0) o << R"(,"ts":)" << ts;
    if (n > 0) o << R"(,"n":)" << n;
    if (flag) o << R"(,"flag":true)";
    o << R"(,"input_size":)" << json.size();
    o << R"(,"depth":)";
    int d = 0, md = 0;
    for (char c : json) { if (c == '{' || c == '[') { d++; if (d>md) md=d; } else if (c == '}' || c == ']') d--; }
    o << md;
    o << R"(,"alnum":)" << std::count_if(json.begin(), json.end(),
        [](unsigned char c) { return std::isalnum(c); });
    o << "}";
    return o.str();
}
